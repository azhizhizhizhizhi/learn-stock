# A股压缩图分析工具

一个基于 React + Canvas 的股票可视化分析工具，使用独特的"压缩图"方式展示股票K线数据，蜡烛的宽度代表成交额大小，高度代表价格区间。

![React](https://img.shields.io/badge/React-19-blue)
![TypeScript](https://img.shields.io/badge/TypeScript-5-blue)
![Vite](https://img.shields.io/badge/Vite-6-purple)
![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-v4-cyan)
![Netlify](https://img.shields.io/badge/Netlify-部署成功-brightgreen)

---

## 🎯 在线演示

**🔗 https://stock.azhizhizhi.top**

---

## 目录

- [功能特点](#功能特点)
- [技术栈](#技术栈)
- [环境要求](#环境要求)
- [快速开始](#快速开始)
- [部署指南](#部署指南)
- [使用说明](#使用说明)
- [压缩图说明](#压缩图说明)
- [项目结构](#项目结构)
- [API说明](#api说明)
- [常见问题](#常见问题)

---

## 功能特点

- 🔍 **全市场搜索** - 支持输入股票代码或名称搜索A股市场所有股票
- 📊 **压缩图可视化** - 独特的成交量加权K线图，直观展示价格与成交额关系
- 📈 **真实数据** - 数据来源于东方财富网API，实时获取
- 🖱️ **交互体验** - 支持缩放、滚动浏览、悬浮查看详情等交互功能
- 📱 **响应式设计** - 适配不同屏幕尺寸

### 图表特性

| 视觉元素 | 代表含义 |
|---------|---------|
| 蜡烛宽度 | 成交额大小（宽=成交活跃） |
| 蜡烛高度 | 价格区间（最高-最低） |
| 红色背景 | 上涨日（收盘 > 开盘） |
| 绿色背景 | 下跌日（收盘 < 开盘） |
| 黄色横线 | 开盘价位置 |
| 蓝色横线 | 收盘价位置 |
| 黑色竖线 | 蜡烛内部纹理 |

### 悬浮提示信息

鼠标悬停在蜡烛上可查看：
- 开盘价、收盘价、最高价、最低价
- 涨跌幅、涨跌额
- 成交量、成交额
- 换手率

---

## 技术栈

| 技术 | 版本 | 用途 |
|-----|------|-----|
| React | 19.x | 前端框架 |
| TypeScript | 5.x | 类型安全 |
| Vite | 6.x | 构建工具 |
| Tailwind CSS | 4.x | 样式框架 |
| Canvas API | - | 图表绘制 |
| Netlify Edge Functions | - | API 代理 |

---

## 环境要求

### 必需环境

| 软件 | 最低版本 | 推荐版本 |
|-----|---------|---------|
| Node.js | 18.0+ | 20.x LTS |
| npm | 9.0+ | 10.x |

### 检查环境

```bash
# 检查 Node.js 版本
node -v

# 检查 npm 版本
npm -v
```

### 浏览器兼容性

| 浏览器 | 最低版本 |
|-------|---------|
| Chrome | 90+ |
| Firefox | 90+ |
| Safari | 14+ |
| Edge | 90+ |

---

## 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/azhizhizhizhizhi/learn-stock.git
cd learn-stock
```

### 2. 安装依赖

```bash
npm install
```

### 3. 启动开发服务器

```bash
npm run dev
```

启动成功后，终端会显示：

```
  VITE v6.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.x.x:5173/
```

### 4. 访问应用

打开浏览器访问 http://localhost:5173

---

## 部署指南

### 方式一：Netlify 部署（推荐，国内可访问）

本项目已配置好 Netlify Edge Functions 用于 API 代理，可直接部署。

#### 步骤 1：Fork 或 Clone 项目到您的 GitHub

#### 步骤 2：在 Netlify 创建站点

1. 访问 [Netlify](https://app.netlify.com)
2. 使用 GitHub 登录
3. 点击 **Add new site** → **Import an existing project**
4. 选择 `learn-stock` 仓库
5. 构建配置（Netlify 会自动检测）：
   - Build command: `npm run build`
   - Publish directory: `dist`
6. 点击 **Deploy site**

#### 步骤 3：等待部署完成

部署成功后，您将获得一个 `xxx.netlify.app` 的网址。

#### 步骤 4：（可选）绑定自定义域名

1. 在 Netlify 站点设置中，点击 **Domain management** → **Add custom domain**
2. 输入您的域名（如 `stock.example.com`）
3. Netlify 会提供 CNAME 地址

4. 在您的域名 DNS 管理处添加记录：

| 记录类型 | 主机记录 | 记录值 |
|---------|---------|--------|
| CNAME | stock | `您的站点名称.netlify.app` |

5. 等待 DNS 生效（通常几分钟到几小时）
6. Netlify 会自动为自定义域名配置 SSL 证书

---

### 方式二：Vercel 部署

1. 将项目推送到 GitHub
2. 访问 [Vercel](https://vercel.com)
3. 点击 "New Project"
4. 导入 GitHub 仓库
5. 点击 "Deploy"

> ⚠️ 注意：Vercel 在中国大陆访问可能需要翻墙

---

### 方式三：Docker 部署

创建 `Dockerfile`：

```dockerfile
# 构建阶段
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# 生产阶段
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

> ⚠️ 注意：Docker 部署需要自行配置 API 代理

---

## 使用说明

### 1. 搜索股票

在顶部搜索框输入：
- **股票代码**：如 `600519`、`000858`
- **股票名称**：如 `茅台`、`平安`

### 2. 选择股票

从下拉列表中点击目标股票，系统会自动获取历史数据。

### 3. 查看图表

- **缩放**：使用顶部缩放滑块或 `+`/`-` 按钮
- **滚动**：当图表放大后，拖动底部滚动条查看不同时间段
- **重置**：点击"重置"按钮恢复默认视图

### 4. 查看详情

将鼠标悬停在任意蜡烛上，会显示详细信息面板：
- 价格信息：开盘价、收盘价、最高价、最低价
- 涨跌信息：涨跌幅、涨跌额（红涨绿跌）
- 成交信息：成交量、成交额、换手率

---

## 压缩图说明

压缩图是一种独特的股票可视化方式，由日本交易员发明，能够同时展示价格和成交量信息。

### 核心原理

```
传统K线图：所有蜡烛宽度相同
压缩图：    蜡烛宽度 = 成交额占比

     ┌───┐
     │   │  宽 = 成交额大（资金关注度高）
     │   │  高 = 价格波动大（多空分歧大）
     │   │
     └───┘
```

### 图表解读

| 形态 | 含义 |
|-----|------|
| 宽大红蜡烛 | 放量上涨，资金积极买入 |
| 宽大绿蜡烛 | 放量下跌，资金恐慌抛售 |
| 窄小红蜡烛 | 缩量上涨，上涨动力不足 |
| 窄小绿蜡烛 | 缩量下跌，抛压较轻 |
| 特宽蜡烛 | 成交额显著高于平均，需重点关注 |

### 黄蓝线说明

- **黄色线（开盘价）**：显示当日开盘位置
  - 当开盘价=最高价时，顶部显示加粗黄线
  - 当开盘价=最低价时，底部显示加粗黄线
  
- **蓝色线（收盘价）**：显示当日收盘位置
  - 当收盘价=最高价时，顶部显示加粗蓝线
  - 当收盘价=最低价时，底部显示加粗蓝线

---

## 项目结构

```
learn-stock/
├── netlify/                    # Netlify 配置
│   └── edge-functions/
│       └── api-proxy.ts        # Edge Function API 代理
├── public/                     # 静态资源
│   └── vite.svg
├── scripts/                    # 工具脚本
│   └── fetchStockData.ts       # 获取真实数据脚本
├── src/
│   ├── api/
│   │   └── stockApi.ts         # 东方财富API封装
│   ├── assets/
│   │   └── react.svg
│   ├── components/
│   │   ├── SearchBar.tsx       # 股票搜索组件
│   │   ├── StockChart.tsx      # 压缩图组件（核心）
│   │   └── StockInfo.tsx       # 股票信息面板
│   ├── data/
│   │   ├── mockData.ts         # 模拟数据（8只A股）
│   │   └── real_600519.ts      # 茅台真实历史数据
│   ├── types/
│   │   └── stock.ts            # TypeScript 类型定义
│   ├── App.tsx                 # 主应用组件
│   ├── index.css               # 全局样式
│   └── main.tsx                # 入口文件
├── .gitignore
├── eslint.config.js
├── index.html                  # HTML 模板
├── netlify.toml                # Netlify 配置文件
├── package.json
├── postcss.config.js
├── README.md
├── tailwind.config.js
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.node.json
└── vite.config.ts              # Vite 配置（开发环境 API 代理）
```

---

## API说明

### 数据来源

本项目使用东方财富网的公开API：

| API | 用途 | 端点 |
|-----|------|-----|
| 股票搜索 | 根据关键词搜索股票 | `/api/suggest` |
| K线数据 | 获取历史K线数据 | `/api/kline` |
| 股票列表 | 获取股票基本信息 | `/api/clist` |

### 跨域处理

#### 开发环境

使用 Vite 开发服务器代理：

```typescript
// vite.config.ts
server: {
  proxy: {
    '/api/suggest': {
      target: 'https://searchapi.eastmoney.com',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api\/suggest/, '/api/suggest/get'),
    },
    // ... 其他代理配置
  }
}
```

#### 生产环境

使用 Netlify Edge Functions 代理：

```typescript
// netlify/edge-functions/api-proxy.ts
// 自动处理 /api/* 请求，添加正确的请求头
```

Edge Functions 的优势：
- 添加正确的 Referer 和 Origin 请求头
- 处理 CORS 跨域
- 支持自定义域名
- 全球边缘节点加速

---

## 常见问题

### Q: npm install 失败？

```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### Q: 图表显示空白？

1. 检查浏览器控制台是否有错误
2. 确认已选择股票
3. 检查网络请求是否成功

### Q: API 请求失败？

1. 检查网络连接
2. 东方财富API可能有频率限制
3. 确认 Netlify Edge Functions 已正确部署

### Q: 自定义域名无法访问 API？

1. 确认 DNS 已生效（CNAME 记录正确）
2. 确认 Netlify SSL 证书已颁发（绿色状态）
3. 等待几分钟让 Edge Functions 生效

### Q: 如何添加新的股票数据？

修改 `src/data/mockData.ts`：

```typescript
export const mockStocks: Stock[] = [
  {
    info: {
      code: '你的股票代码',
      name: '股票名称',
      market: 'SH', // 或 'SZ'
      industry: '行业',
      totalShares: 总股本,
      floatShares: 流通股本,
    },
    data: [/* 股票数据数组 */],
  },
  // ...其他股票
];
```

---

## 注意事项

⚠️ **重要声明**

- 本工具仅供学习和研究使用
- 数据仅供参考，不构成任何投资建议
- 股市有风险，投资需谨慎
- API调用可能受到频率限制

---

## License

[MIT](LICENSE)

---

## 贡献

欢迎提交 Issue 和 Pull Request！

---

## 致谢

- 数据来源：[东方财富网](https://www.eastmoney.com/)
- 部署平台：[Netlify](https://www.netlify.com/)
